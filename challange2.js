// return number of demerit points if speed is above 70 but under 170

let rightSpeed = 70 
let demerit = rightSpeed=+5
let demeritpoint = 1

 while (rightSpeed = demerit && rightSpeed<=170) {
    console.log ("point"+ demeritpoint++)
 }

 if (rightSpeed > 171 ) {
    return "Lisence Suspended"
 };
 
